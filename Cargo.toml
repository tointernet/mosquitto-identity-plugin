[package]
name = "mosquitto-identity-plugin"
version = "0.1.0"
edition = "2021"

[lib]
crate-type=["dylib"]

[dependencies]
libc = "0.2.133"
log = "0.4.17"
env_logger = "0.9.1"

[build-dependencies]
bindgen = { version = "0.60.1", default-features = false, features = ["runtime"] }